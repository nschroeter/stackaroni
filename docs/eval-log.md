# Evaluation log

Running record of experiments against the fixed test stacks in `test-data/`.
Append a new row after any change that could affect output quality — don't
edit or delete old rows, even if the approach was later abandoned; the
history of what didn't work is as useful as what did.

**Also append a row for a change proven *not* to affect quality.** Every rating
here was given to output from the code as it stood at the time, so any change to
a pipeline stage puts every rating above it in question until something shows
otherwise. The proof is what keeps those ratings valid, and it belongs next to
them — a reader asking "does the 5 still apply?" should not have to reconstruct
the answer from commit messages.

**Keep "Current state" below in sync in the same commit as the row that
changes it.** Append-only chronology records what happened but not what is
true now, and past ~15 rows that becomes unreadable: a row can be retracted
two rows later and still read as open. This happened — a halo finding logged
as "needs Niels's rating" stayed that way after being retracted and closed
further down. Updating the block is part of logging, not a follow-up. A row
changes what is live when it: ships a new parameter set, retracts or
supersedes an earlier claim, or resolves an open question.

Score is 1-5 against the quality checklist in `CLAUDE.md`:

1. No visible seams/halos around high-contrast edges (antennae, legs, hair boundaries)
2. No ghosting from misalignment
3. Background bokeh stays smooth, not patchily sharpened
4. Consistent color/exposure across the fused image

Log one row per stack tested if scores differ between stacks (they often
will — e.g. the synthetic stack's thin antenna lines tend to expose halo
problems that the blossom stack won't).

## Current state

*Updated with T15, per-frame coverage in fusion. Update this block in the same commit as
any row that changes it.*

**Live ratings, on a verified-clean set (2026-08-15): 5 / 5 / 5**
(synthetic_50 / ruler / blossom), on the only pipeline there is. **Re-run and re-rated on
merged `main` at `361504c`** after the wavelet removal: the outputs came out
**bit-identical** to the rated ones, 0 differing pixels on all three stacks, and scored
5 / 5 / 5 again. **The remaining softness in blossom's upper-right corner is the capture,
confirmed by Niels against the measurement — no frame resolves it.** **Full field of view
is kept deliberately** — see the T15 row for what a rebase would have cost, and the
2026-08-15 confirmation row for how that cost was nearly paid by accident. **`wavelet`
rated 2 / 4 / 2 and was removed in T17** — every earlier `wavelet` rating is superseded and
none of them describes anything that still ships. The two stacks whose backgrounds are
smooth enough to show the DSE fringe scored 2; `ruler`, whose texture hid it, scored 4.

**T15 itself is rated and live under `local`: blossom 5 and ruler 5.**
Frames now compete only inside the region they can actually fill, so border-replicated
pixels can no longer be selected. **Field of view, framing and dimensions are unchanged**,
so crops still line up with every earlier output, and the change reaches the margins only —
106 of 1.08M pixels on synthetic_50, a 32 px inset identical. **Blossom under `wavelet` was first rated
here at 5 and has since been re-rated 2 on a clean set — see the 2026-08-15 rating row.**

**`output_is_stable` now pins `0xee22e1aa7efbdf2f`.** The constant moved because the rating
came first, which is the rule; synthetic_50 was not separately re-rated and the margin-only
measurement above is what stands in for it.

**Do not call T15 a strict improvement.** synthetic_50 has a ground truth, so it can be
measured rather than rated, and measured it is slightly *worse*: RMSE 556.509 → 559.639,
worst per-pixel 17634 → 21314, all of it outside a 32 px inset where the two outputs are
identical (583.398 → 583.400 inside). The cause is inherent, not a defect — at the extreme
edge the sharpest frame for a position may be one that cannot cover it, so excluding it
falls back to a less sharp frame, and the sharp data exists at no valid coordinate. It is
accepted because blossom and ruler, the stacks the rubric exists for, rate 5/5 under both
methods.

**`wavelet` no longer exists — removed in T17 (row below).** There is one pipeline:
registration → focus → weights → pyramid fusion, rated 5/5/5. `local` is no longer a
*choice*, so `--method` and the app's method chooser are gone too. The paragraph below is
kept because it is why the method was removed.

**The defect that removed it: the defocus spread effect (DSE).** A
defocused object's blur spreads past its true silhouette, so at a background pixel just
outside the subject the *defocused* frames carry real signal while the sharp ones carry
flat background — and an activity argmax picks the defocused one, dragging subject colour
outward. Visible as an orange fringe around synthetic_50's ellipse. **`local` is clean at
the same pixels**, and on the only stack with an objective answer beats wavelet 559.6 to
846.1 RMSE against ground truth. **Neither of this repo's named follow-ups fixes it** — the
undecimated transform addresses shift-variance, graph-cut adds a smoothness prior, and this
band is coherent and smooth already. **And the published fix aimed straight at it (Kim 2023,
focus-axis brightness symmetry) was implemented in full and measured inert — see the T16 row.
Three named approaches are now ruled out; read those rows before starting a fourth.**

**Rejected on the way, and worth not re-deriving: rebasing registration onto a canvas every
frame covers.** It removes the streaking too, and Zerene's PMax appears to do exactly that.
It costs field of view equal to half the stack's magnification range — 5.25% on ruler,
5.57% on blossom — which is too much to charge a stack like ruler that has the same
geometry and no visible defect. **The streaking is content-dependent, not
geometry-dependent**: smooth margins expose replicated pixels, textured margins outcompete
them.

**There are now two pipeline shapes, not one (T14).** `core::pipeline::Method` selects
between `Local` — registration → focus → weights → pyramid fusion, the four stages every
rating below was given to — and `Wavelet`, which keeps registration and collapses the other
three into coefficient selection (`docs/algorithms.md` §5). **`Local` is still the default
and is byte-identical**: `output_is_stable` still hashes synthetic_50 to
`0x00455c66dd1e4c95`, so **no rating below is affected**. Wavelet is `--method wavelet` and
a "Method" combo box in the app. **Rated under `wavelet`: ruler 5, synthetic_50 5, and —
after T15 removed the margin streaking found while reviewing it — blossom 5.** Wavelet now
matches `local` on every stack in the set.

**The frame-margin streaking is `warp_frame`'s border clamp reaching fusion, not either
fusion rule's own doing, and it is fixed in T15.** Coloured stripes along the top and right margins, ~230 px in at the sides and ~156
px at top and bottom on blossom. Present in `Local` and `Wavelet` at the same pixels and
the same strength, absent from the source frames, and the difference map between the two
methods is black across the whole affected region. Cause: `register_stack` anchors on the
*middle* frame, so the output canvas is the middle frame's field of view, and every frame
more magnified than it (blossom registers 0.9495..1.0569) must be sampled outside its own
bounds — `warp_frame` border-replicates there, and both selection rules then compete over
the replicated strips. **Zerene's PMax reference does not do this**: full 8664x5784 canvas,
clean to the last pixel, at scale 0.930 relative to ours — i.e. anchored so that every
frame covers the canvas. Fix is T15. **This supersedes the T14 row's "pre-existing warp
behaviour" note**, which was correct that wavelet did not introduce it and silent on why.

**The four-stage decomposition is now known not to be universal.** Wavelet needed a fifth
trait, `StackFusion`, because there is no per-frame `FocusMap` to hand a `WeightEstimator`
and no `WeightMaps` to hand an `ImageFusion` — the quantities live in a coefficient domain
neither type can represent. Registration stayed outside it and is still shared. See the
stage-boundary note in `CLAUDE.md`, which also records that `Select` already re-measures
salience inside `fuse` and uses the weight maps only at the coarsest level.

**The fusion rule is one type as of T13** — `core::fusion::FusionKind`, a data-carrying enum
owning construction, each rule's own parameters, and the CLI token / UI label / trade-off
summary. **The app offers only `Local` (selection); `blend` is CLI-only.** The CLI's `FusionArg`, the app's
`FusionRule` and `Settings.select_fusion` are gone. `--fusion select|blend` is unchanged, so
every row below still reproduces. `the_app_and_the_cli_agree_byte_for_byte` now holds the
app and the CLI to identical bytes on both rules.

**Live configuration** — **fusion rule `select`, salience radius 2 — now the default**;
guided filter radius 4, epsilon 1e-4, guide space perceptual; focus radius 4; registration
phase-correlation similarity at level 3, chained outward from the middle frame; pyramid
floor 32 px. **Every row above the T11 rows was produced by the blend and now needs an
explicit `--fusion blend` to reproduce.**

**Current outputs** — `target/debug-out/t11/{blossom_select,ruler_select,synthetic_50_select}.tif`.
Comparison crops: `t11/crops/` (blossom, `blend | select | pmax`), `t11/crops-ruler/`
(`blend | select`), `t11/crops-synthetic/` (`blend | select | truth`). The older
`test-data/<stack>/stackaroni_fused.tif` and `t10/` outputs are **blend** results and are
now superseded.

**Rated, all three stacks, under `select`** — **blossom 5** (was 1, and better than the
Zerene PMax reference), **ruler 5** (was 3), **synthetic_50 4** (was 5). Nothing is
currently awaiting a human score. **Not a clean sweep — see the trade-off row; do not
summarize it as one.**

**Closed: synthetic_50's residual antenna softness was the fixture, not the pipeline.**
Niels's direct read — it is the staircase aliasing in the ground-truth render, the same
rendering artifact already excluded from the score at `2fbf990`. **Do not run the
ground-truth-warp-and-remeasure step**; the thread is closed, not paused. The measurements
taken along the way stand and are still true (salience window refuted, `warp_frame`
resampling unquantified) — they simply stop motivating work.

**What is left untested, and why most of it cannot help where it sits.** Audited against
`docs/algorithms.md` on 2026-08-16. Genuinely unbuilt and in scope: **§8 MRF / graph-cut
weight refinement** (the executive summary rates it 5 stars) and **§3's other focus
measures** — Tenengrad, gradient energy, local variance; only `WindowedLaplacian` exists,
though the `FocusMetric` trait was built for exactly this. Contraindicated or out of scope:
ECC affine (its trigger has not fired — rotation measures ≤0.124°), ORB+RANSAC (phase
correlation works), deep learning.

**Both remaining candidates sit in stages the shipped fusion rule mostly ignores, which is
the T18 finding applied forward.** Focus maps feed the weight stage; under `select`,
weights reach only the *coarsest* pyramid level, because every band-pass level is decided
by `select_more_salient` from the Laplacian coefficients directly — which reads neither the
focus measure nor the weights. Measured: swapping the entire weight family moved 931 pixels
of 1.08M. So a graph-cut weight refiner would refine weights that barely reach the output,
and a better focus measure would feed weights that barely reach the output. **Neither is
worth building against `select` without first changing what consumes them.** The leverage
is in stage 4 — the selection rule itself or its salience measure — which is also where the
7.6-effective-frames figure is actually decided.

**The search for a second method is closed (T18).** Three attempts have failed: wavelet
2/4/2, the published DSE fix measured inert, and depth-map weights 2/3/1 — each against
`local`'s 5/5/5, which itself beat a mature commercial PMax. **Do not start a fourth
without reading the T14, T16, T17 and T18 rows.** What remains open are measured gaps
*inside* `local`: ~7.6 effective frames averaged per pixel against an ideal of 1-3, and
detail at 91% of the per-pixel oracle on synthetic_50. Those are improvements to a method
that works, not replacements for it.

**Multi-scale focus measurement is a closed dead end, not an open option (T12).** It was
built in full and measured inert on this pipeline — detail 0.330 at *every* scale count and
decay setting on synthetic_50, 0.02% RMSE on blossom. **It is not in the shipped surface:
no `--focus` flag, no UI entry, no `MultiScaleLaplacian`.** The code sits unmerged on
`t12-multiscale-focus` and `docs/algorithms.md` §4 carries the write-up. **Do not read §4's
opening paragraph as an outstanding idea** — it is the argument that motivated this and it
did not survive contact with the test stacks. A revisit means **scale-normalized** levels
(Lindeberg 1998), which is a different algorithm on a new branch, not this code retuned.

**Runtime** — a 100-frame 50 MP stack takes **~4.4 minutes**, down from ~20.4. Fusion is
62% of that and the least parallel of the four stages, deliberately: its per-frame loop
stays sequential to keep float ordering, so only its inner loops went wide. **Any change
to a pipeline stage must keep `tests/output_is_stable.rs` passing** — it hashes the fused
output of synthetic_50 and is what makes "this is only a speedup" checkable rather than
asserted.

**Colour noise is closed as a fusion question.** `select`'s chroma noise is one source
frame's, passed through — not manufactured by per-channel or cross-level selection, both
ruled out. The residual gap to PMax (~12 pp Cb, ~9 pp Cr at matched brightness) is a
denoising difference, and about half the *apparent* gap is PMax's brighter tone rather than
noise at all. Any future work here belongs in a denoising thread, not in `fusion.rs`.

**Closed: ruler's lifted black point was not a separate thread.** It was the blend mixing a
tick's surroundings into it at coarse pyramid levels, and `select` fixes it (p1 0.291 →
0.119 at unchanged p99). The transfer function was never at fault; `quantize()` in
`tiff_io.rs` is exonerated. Do not reopen this as a tone-mapping investigation.
**Corroborated independently:** Zerene's PMax on the same frames reaches tick contrast
0.3317 against our 0.3164, so we sit at 95% of a mature implementation of the same family —
a result a transfer-function bug on our side could not produce.

**Reference renders are in place for both real stacks** — `test-data/{blossom,ruler}/reference_pmax.tif`,
both used, both documented in `test-data/README.md`. Their purpose has narrowed now that
T11 closed the architectural gap they were bought to diagnose: they answer "where does a
mature implementation still differ?", not "is this achievable?". **Neither shares our
coordinate system** (blossom scale 0.930, ruler 1.065 against ours) despite identical canvas
sizes — resample before comparing crops or the panels show different content.

**Scores in the log that are NOT live** — `51d6833` (1-2) and `6e638d7` (3 provisional)
both score the superseded radius-8/eps-1e-2 configuration, and both are my own reads, not
Niels's. The `e2e6b8a` halo row reads "needs Niels's rating" but was retracted and closed
below it; nothing there to rate. **All `e2e6b8a` scores (5/3/1) are now superseded** — they
rate the blend, which is no longer the default.

**Recorded plainly, and now resolved** — the r4 retune's measured gains were real (signal
0.32 -> 0.45, effective frames 25 -> 7.6) and blossom stayed rated 1 anyway. That was logged
at the time as evidence the metrics tracked something real but insufficient, and that the
remaining problem was architectural. **T11 confirmed it: the same parameters, one change to
the fusion rule, 1 -> 5.** The metrics were right about the parameters and silent about the
architecture. Keep the original lesson — do not let a metric gain stand in for a rating —
and add its converse: a metric that refuses to move is not proof the parameters are wrong.

**Open, not blocking** — T5b item 1, chained registration drift (measured secondary,
~11% of degradation); ~7.6 effective frames averaged per pixel on blossom against an
ideal of 1-3; a ring of high-index labels at the subject boundary, traced to the focus
window straddling depth discontinuities rather than to fusion.

**Standing caution** — and one that keeps recurring in a new disguise: **no
high-frequency energy measure settles a sharpness question on noisy data.** In T18 the
depth-map output measured 2.3x the Laplacian SD of the blended one and was reported as
"sharper-looking"; Niels rated the same file 1/5, completely unsharp. The metric was
counting grain. Seven measurement errors so far: four overstating a defect, one
asserting a defect that did not exist, and one (the antenna softness) measuring a property
of the *fixture* rather than of the output, repeatedly, after the fixture's artifact had
already been named and excluded from the score. A claim of a *new* defect needs both a
measurement and a look at the image before it goes in the log — and, on the synthetic
stack, a check against `test-data/README.md`'s known quirks first.

| Date | Commit | Stack | Change | Score (1-5) | Notes |
|---|---|---|---|---|---|
| 2026-08-15 | *discarded, branch deleted* | all three | **T18: a second *method* — depth-map weights, the DMap family against `local`'s PMax family — built, rated, and thrown away. Do not build it again without reading this row.** Cost-volume filtering (Rhemann, Hosni et al., CVPR 2011 / TPAMI 2013): filter each frame's focus map with the existing guided filter, then winner-take-all, giving **one frame per pixel** instead of a soft blend. Compositing rationale from Agarwala et al., *Interactive Digital Photomontage*, SIGGRAPH 2004. Offered as a one-click `Method: Blended | Depth map` in the app and `--method blended|depth-map` in the CLI, default unchanged. | **depth-map: synthetic_50 2, ruler 3, blossom 1** (Niels), against blended's **5 / 5 / 5**. It does not beat `local` anywhere, which was the agreed bar | **Why it loses, in Niels's words:** blossom "completely unsharp"; ruler "low contrast between the letters and the rules, which is perfect in the blended variant"; synthetic_50 "texture in the centre washed out, some halos and ghosting, quite similar to the (dropped) wavelet approach". The mechanism is the same one that sank wavelet: **committing to one frame per position forfeits the sqrt(N) noise averaging a blend gets for free, and pays for it again wherever the label is wrong.** Corroborated objectively where a ground truth exists — synthetic_50 RMSE 735 (select) and 815 (blend) against blended's 559. **Three architectural findings worth more than the method was.** **(1) A `WeightEstimator` cannot be a second method while `select` ships.** Under the shipped fusion rule, weights reach only the *coarsest* pyramid level — every band-pass level is re-decided from the coefficients. Measured: swapping the entire weight family changed **931 pixels out of 1.08M** on synthetic_50. This is the stage-3/stage-4 boundary `CLAUDE.md` documents, and it constrains the shape of any future second method: it must own the fusion rule too, or it is inert. **(2) The guide of a cost-volume filter must be one shared image, not each frame's own.** The guided filter returns `q = a*I + b`, locally linear in the *guide*, so a per-frame guide expresses each slice's cost in that frame's appearance and the cross-slice argmax compares different units. Built that way first, it produced an output that had lost content outright. Fixed to the anchor frame; the fix was correct and moved RMSE by less than 2, so it was a real bug and not the reason the method loses. **(3) Laplacian SD is not a detail metric on noisy stacks, and it misled this row's author.** The blossom depth-map output measured **2.3x** the Laplacian SD of the blended one and I reported it as "sharper-looking but grainier". Niels rated the same file 1/5, completely unsharp. The metric counts high-frequency energy and cannot separate detail from grain; here it was measuring grain alone. **Add it to the standing-caution list: no high-frequency energy measure settles a sharpness question on ISO-1600 data.** **The strategic conclusion, recorded so it is not re-litigated every session.** This is the third method attempt to fail: wavelet 2/4/2 (T14, removed T17), the published DSE fix measured inert (T16, discarded), and now depth-map 2/3/1. `local` beat a mature commercial PMax on blossom. **Nothing classical found so far comes near it, and the search for a second method should stop.** Effort is better spent on the measured gaps *inside* `local` — 7.6 effective frames per pixel against an ideal of 1-3, and detail at 91% of the per-pixel oracle on synthetic_50 — which are improvements to a method that works rather than replacements for it. |
| 2026-08-15 | `361504c` | all three | **Confirmation run on merged `main` after the wavelet removal — no change, and that is the point.** The documented eval command (`--test-set test-data --debug-out …`) across all three stacks, on the clean set: frame counts **100 / 100 / 50**, registration ranges blossom 0.9495..1.0569, ruler 0.9535..1.0542, synthetic_50 0.9994..1.0063 — all unchanged. Timings blossom 257s, ruler 245s, synthetic_50 4s. Outputs, per-stage timings and debug artefacts in `~/Desktop/stackaroni/t17/`. | **Re-rated anyway: synthetic_50 5, ruler 5, blossom 5** (Niels, on the t17 outputs). The files are **bit-identical** to the ones already rated — `magick compare -metric AE` returns **0 differing pixels** on all three — so this is the same judgement on the same pixels, deliberately taken a second time after a 1365-line removal. synthetic_50 still measures RMSE **559.639** against ground truth | **What this actually establishes, since a run that changes nothing is easy to dismiss.** T17 deleted 1365 lines, removed the `Method` and `StackFusion` abstractions, and rewired both front ends; `output_is_stable` proves only that *synthetic_50* is unchanged. This checks the other two stacks, at full size, through the real CLI path rather than through a test harness — the three-stack equivalent of the hash gate. **A refactor that big is exactly the kind that moves a stack the gate does not hash.** **Two observations from the rating, both closing open threads.** **"I really like that the result is NOT cropped like Zerene does."** The rebase built first would have cost 5.25% of the frame on ruler and 5.57% on blossom, and Zerene's own reference is a full-size canvas at scale 0.930 against ours — the same trade expressed as magnification instead of as a crop. **How that cost came to be paid is the part worth recording, and it is a process failure, not an instinct.** The rebase was chosen from an options table that described it as keeping the "output canvas full size" — true of pixel dimensions, misleading about field of view, which is the thing that matters. Niels selected it on that description, then saw the result and said it looked cropped; the field-of-view loss was measured only afterwards. **He did not reject the rebase in advance and should not be recorded as having done so. The option was mis-described when offered, and the output is what caught it.** The lesson is about how choices are presented: state a trade in the units the person cares about, not the units that make it sound free. **"The only unsharpness is in the upper right corner, but the input was unsharp there too."** Independent confirmation of the measurement taken when that corner was first reported: per-frame detail there declines monotonically 0.180 (frame 1) → 0.153 (frame 96) with no peak, and frame 1's corner is featureless blur. **The corner is the capture, not the pipeline — that thread is closed by agreement between a measurement and an eye, which is the strongest form this log has.** **Recorded as a dated confirmation rather than folded into the T17 row** because it is evidence gathered after that change was merged, not part of it: the T17 row claims the removal was output-neutral, and this row is what makes that claim checkable rather than asserted. |
| 2026-08-15 | *see below* | all three | **T17: the wavelet method is removed from the codebase.** `core::wavelet` (875 lines), the `StackFusion` trait, the `Method` enum, `--method`, `--consistency-threshold` and the app's method chooser are all gone. There is one pipeline again: registration → focus → weights → pyramid fusion. **Why: it rated 2/4/2 against `local`'s 5/5/5** on a verified-clean set, its defect was diagnosed (defocus spread effect, the row two below), and the published fix for that defect was implemented in full and measured inert (T16). Three named remedies are ruled out. Keeping a second method that is worse on every stack, and that the CLI and UI had to caveat in every description, is carrying cost for no case where anyone should pick it. **`local` is provably untouched: `output_is_stable` still hashes synthetic_50 to `0xee22e1aa7efbdf2f`**, unchanged across the removal — which is the whole point of that gate. Every rating in this log stands. | **no new rating — nothing changed about the output.** The hash gate is the evidence | **What is lost, stated plainly so it is not rediscovered as a surprise.** T14's finding — that the four-stage decomposition is *not* universal — was demonstrated by wavelet needing a fifth trait. **The demonstration is now prose only**, in `CLAUDE.md`'s stage-boundary section and `docs/algorithms.md` §5, both updated to say so. The finding stands; the code that proved it is in `git log`. **What the tests lost, and what replaced it.** `the_panel_swaps_with_the_method` asserted that a knob for a stage the selected method never runs is *absent* — the harder and more valuable half of a UI test. With one method there is no swap, so it became `the_panel_offers_every_knob_the_pipeline_reads`, which keeps the positive assertions and adds absence checks for the removed chooser. **If a second method is ever added, restore the absence assertions with it.** `the_app_and_the_cli_agree_byte_for_byte` drops from three configurations to two. **Not removed:** `--fusion blend`. It is worse (blossom 1/5) and stays CLI-only, but it reproduces older eval-log rows, which is a job the headless runner exists for. **Reversal cost, since this is the kind of decision that gets revisited:** the method is one `git revert` away plus reinstating `StackFusion` and `Method`; the eval-log rows describing its behaviour are append-only and remain. |
| 2026-08-15 | *discarded, branch deleted* | synthetic_50 | **T16: the published fix for the DSE fringe was implemented in full, measured inert on this pipeline, and thrown away. Do not implement it again.** Kim, *Edge Bleeding Artifact Reduction for Shape from Focus in Microscopic 3D Sensing*, Sensors 2023 (PMC10611386). It weights each frame's activity by how *symmetric* local brightness is along the focus axis around it: `α = Σ(B_{d+i}+B_{d-i}−2B_d)²`, `β = Σ(B_{d+i}−B_{d-i})²`, `w = (α+ηγ)/(α+ηγ+β)`, argmax on `w·activity`. The premise is that a position's own defocus curve is symmetric about its true focus, while a neighbour's blur arrives from one side in depth only. **Implemented faithfully, not approximated.** Brightness came from the per-level approximation bands — which *are* the local mean at exactly the label grid's scale — averaged over the same `box_sum` window the activity measure uses, as the paper requires. A sliding window of 2γ+1 frames with decisions γ behind the read head; the window narrows at the stack ends rather than mirroring frames that were never photographed. `γ=0` and large `η` were exact off switches, verified: `γ=0` reproduced the baseline hash-for-hash at RMSE 846.098. **Measured, synthetic_50, against ground truth. Baseline (off): whole-image 846.1, rim 944.3.** Attempt 1, γ∈1–3 at η=1e-4: 850–852 whole, worse. Attempt 2, η swept 1e-8..1e-2 and γ to 12: best rim **932.4** (−1.3%) bought with whole-image 858.6 (+1.5% worse). Attempt 3, brightness windowed to the FM footprint per the paper: rim 938.7, whole 854.1. **At 2.6x saturation the orange fringe is visually unchanged in all of them.** | **no rating — it never earned one.** Best case was a ~1% rim gain against a ~1.5% whole-image regression | **Why it failed, diagnosed rather than guessed.** The weight was *active* — instrumented at mean 0.48 with half of every level below 0.5 — but not *selective*. Dumping the weight as an image showed speckle across the whole frame: `α` and `β` were driven by noise, not by defocus. There is no `η` that discriminates the rim, because on this data the rim's brightness asymmetry is not distinguishable from the background's. **The authors' own stated limitation is the whole story here** — "depth estimation accuracy in the weak-texture areas was lowered" — and our backgrounds are smooth bokeh, which is weak texture everywhere. **A citation error of mine, corrected in the record.** The paper's headline 65.8% RMSE and 85.3% artifact-width reductions are improvements in **depth-map** accuracy: shape-from-focus is a depth-estimation task. They are not claims about fused-image quality. I cited them as if they predicted what a fused image would gain, and that inference was mine, not theirs. **Anyone reading those numbers as a promise about image quality is repeating my mistake.** **Cost if it had been kept:** ~0.5 GB of resident memory for the frame window and a γ-frame delay before any label can be decided. **Stopped at three attempts by the rule agreed before starting**, not because the search was exhausted. **If this is ever revisited, the gap to close first is the discrimination, not the tuning:** find a per-position statistic that separates a neighbour's encroaching blur from the background's own noise on smooth bokeh. Until something does that, no weighting built on `α`/`β` will help here. The DSE row above still stands as the diagnosis, and `local` remains the answer — 5/5/5 against wavelet's 2/4/2. |
| 2026-08-15 | `693950a` | all three | **Full re-rating on a verified-clean test set, both methods — the first since the set was found contaminated.** Two fused results had been saved into their own stack directories (`blossom_stacked_local.tif` 20:27, `ruler_stacked_local.tif` 20:03) and were being stacked as 101st frames; see the guard row. These six outputs were produced after they were removed, with frame counts checked in the run log: **50 / 100 / 100**. Outputs and per-stage timings: `~/Desktop/stackaroni/`, `run-log.txt` beside them. | **local: synthetic_50 5, ruler 5, blossom 5. wavelet: synthetic_50 2, ruler 4, blossom 2.** | **`local` is a clean sweep and is the default — this is the strongest position the pipeline has been in.** **Every earlier `wavelet` rating is superseded.** Wavelet previously held 5 on all three stacks (T14 for ruler and synthetic_50, T15 for blossom); on a clean set and a closer look it is 2 / 4 / 2. **Do not cite the old wavelet 5s.** **Two causes are confounded here and the log should not pretend otherwise.** The earlier ratings were given (a) on runs that may have included the fused 101st frame, and (b) before the orange rim fringe had been pointed out and named. Which of the two moved the numbers is not separable from the evidence available — what is certain is that the new ratings are on a clean set with the artifact known. **The DSE row directly above is no longer just a diagnosis.** It now has ratings behind it: the two stacks where the fringe is visible against smooth backgrounds score 2, and `ruler` — whose textured surface hides it, the same property that spared it the margin streaking — scores 4. That is the artifact's signature, not a coincidence. **Consequence for the shipped surface:** `wavelet` is offered in the CLI and the app as an alternative, and on this evidence it is not a peer of `local`. It has not been removed, because the T14 finding it was built for — that the four-stage decomposition is not universal — stands on its own and the code documents it. But nothing should present the two as equivalent choices. **`output_is_stable` is unaffected and arguably better supported:** it pins the `local`/`select` path, which just scored 5 on all three stacks. |
| 2026-08-15 | *no code change* | synthetic_50 | **Finding, not a change: the orange fringe outside the subject under `--method wavelet` is the defocus spread effect (DSE), and none of the follow-ups this repo already names would fix it.** **What DSE is.** A defocused object's blur spreads *outward* past its true silhouette, so the same object is physically larger in a frame where it is out of focus. That halo lands on pixels belonging to the background. **Why it defeats coefficient selection.** At a background pixel just outside the subject, the frames where the subject is sharp hold flat background — no detail — while the frames where it is defocused hold the halo's intensity ramp, which is real signal. An activity argmax therefore selects the *defocused* frame and drags subject colour into the background. The rule is not malfunctioning; it is correctly answering "which frame has the most signal here", which is the wrong question at a depth boundary. **Evidence.** Level-0 label map at the ellipse rim: a *coherent* band with mean frame index **0.702**, against 0.437 inside the subject and 0.510 (random speckle) in open background — it systematically selects late frames. Source frames confirm the mechanism directly: frame 35's orange halo spreads visibly further than frame 22's at the same position. **Ruled out, with numbers.** `--consistency-threshold` 2 / 4 / 6 gives RMSE 846.8 / 846.1 / 846.1 against ground truth — the paper's own majority filter does nothing, because it removes *isolated speckle* and this band is coherent. **That same property rules out both follow-ups already written into this repo:** the undecimated transform named in `wavelet.rs`'s module docs addresses shift-variance, which is blocky and incoherent; graph-cut refinement (`docs/algorithms.md` §8) adds a *smoothness* prior, and the band is already smooth. It is smooth and wrong. **Do not spend a session on either expecting this to improve.** **Scope.** `wavelet` only — `local` is clean at the same pixels. Present identically in the T14 and T15 outputs, so it pre-dates the coverage work and is not a regression from it. Visible on `synthetic_50` at the ellipse rim; it took ~2.6x saturation to make it obvious, which is why it survived a 5 rating. | **not a rating — a diagnosis.** The objective measure stands as the summary: on synthetic_50, RMSE against ground truth is **local 559.6 vs wavelet 846.1** | **A fix means a new fusion method, not a patch.** The classical, non-deep-learning route is Xu et al., *Towards Reducing Severe Defocus Spread Effects for Multi-Focus Image Fusion via an Optimization Based Strategy*, IEEE TIP (arXiv:2012.14678), which replaces per-coefficient argmax with an optimisation requiring each local output region to resemble the sharpest source region; also a region fusion model with explicit boundary correction (Information Fusion, S1566253524000083). The α-matte boundary defocus model (arXiv:1910.13136) is a CNN and therefore out of scope per `CLAUDE.md`. **Recommendation recorded so the trade is not re-argued from scratch:** this is a real limitation of the weaker branch. `local` is the default, does not show it, and wins on the only stack with an objective answer. Build DSE correction only if wavelet is wanted as a *primary* method, and treat it as its own task with its own plan. **Related, and probably the same root cause seen from the other side:** the open T5b item below — "a ring of high-index labels at the subject boundary, traced to the focus window straddling depth discontinuities" — is the `local` pipeline making the same wrong selection at the same places. There it never became visible, because blending dilutes it where selection does not. |
| 2026-08-14 | *see below* | all three | **T15: frames no longer compete outside the region they can actually fill, which removes the frame-margin streaking from both methods at no cost to field of view.** Each frame carries its covered region — an axis-aligned rectangle, since `Transform` is a similarity, so four numbers rather than a mask plane. `select` and the wavelet activity argmax skip positions outside it; `blend` skips them and rescales the affected positions by the weight that actually landed. **The streaking was never wavelet's, and never really registration's either — it is `warp_frame`'s border clamp reaching fusion.** The canvas is the middle frame's field of view, so every magnified frame must be sampled outside its own bounds, where the clamp replicates an edge pixel. A replicated strip is constant along one axis while still carrying the border's texture along the other, so it holds real activity and wins wherever the true content is smooth. **Content-dependent, not geometry-dependent — this is what picked the fix.** `ruler` has the same magnification range (0.9535..1.0542) and the same replicated pixels and shows *no* streaking: its textured margins outcompete the border, so the bad pixels are never selected. Only smooth margins expose it. **Rebasing the reference was built first, measured, and rejected.** It works, and Zerene's PMax appears to do it (full-size canvas at scale 0.930 against ours, clean margins), but it costs field of view equal to half the stack's magnification range — **5.25% on ruler, 5.57% on blossom, measured** — and ruler was already rated 5/5 with no visible defect. Paying that everywhere for a defect some stacks do not have is the wrong trade. Full reasoning in `docs/algorithms.md` §10. **Measured, blossom, right-edge chroma anisotropy `mean|dx|/mean|dy|` on a\*:** 0.796 → 1.014, 0.741 → 1.022, 0.887 → 1.029 across three margin regions — coherent horizontal replication becoming isotropic noise. **Plain SD is the wrong statistic here and was misleading for several rounds:** margin chroma SD *rises* (0.0346 → 0.0394, 0.0416 → 0.0577), because the margin is now stacked from fewer frames and SD cannot tell random noise from the coherent striping it replaced. Luminance metrics moved not at all — the streaks are chroma. **Trust the directional metric and the 3x crops, not SD.** **Field of view and framing are unchanged**: blossom still registers 0.9495..1.0569, output is 8664x5784, and a central 400x400 crop is identical to the pre-change output to 5e-9. **`output_is_stable` fails by design: synthetic_50 now hashes 0xee22e1aa7efbdf2f against the pinned 0x00455c66dd1e4c95. The constant has deliberately NOT been moved** — the gate's rule is that a rating comes first. | **blossom 5, ruler 5 — both methods, both stacks** (Niels, 2026-08-14). blossom under `wavelet` was previously unrated; blossom under `local` holds its T11 5. **synthetic_50 not rated but *measured*, and it regresses slightly: RMSE against ground truth 556.509 → 559.639**, worst per-pixel 17634 → 21314, entirely outside a 32 px inset (583.398 → 583.400 inside) | **The change reaches only the margin, and that is measured rather than asserted:** on synthetic_50, 106 pixels differ from the pre-change output out of 1.08M, and a 32 px inset is identical. **That number was 48,447 before a real mistake was found and fixed.** Trimming the covered region per pyramid level sounds equivalent to trimming once and is not: two coefficients at level `L` are `2·2^L` full-resolution pixels, so it compounds geometrically and reached ~128 px by level five — excluding a 120 px band of valid data on a stack whose true shortfall is ~4 px. `EDGE_TRIM` is now applied once at full resolution, pinned by `reducing_repeatedly_does_not_compound_the_inset`. **A second bound was wrong in a way that would have hidden:** `BILINEAR_MARGIN` of 2 px looks safer than 1 and is not — at `size - 1` the second bilinear tap clamps but is weighted by `fx = 0`, so the sample is exact. The tighter value marks the last row and column uncovered for *every* frame including the identity anchor, and a pixel no frame covers fuses to black. Pinned by `the_identity_covers_the_whole_frame`. **Interior pixels are untouched by construction, not by luck:** renormalization is confined to positions outside the intersection of every frame's coverage, because inside it the weights already sum to one — to within the rounding of a hundred additions, which is exactly why dividing there would perturb pixels nothing was skipped for. **Known cost, not a defect, and now quantified on the one stack that can be:** the outer band is stacked from fewer frames, so its depth of field is shallower and its chroma noise higher. On synthetic_50 that costs RMSE 556.509 → 559.639 against ground truth, all of it outside a 32 px inset. At the extreme edge the sharpest frame for a position may be one that cannot cover it, and the sharp data then does not exist at any valid coordinate — so this is a floor, not something to tune away. Accepted because blossom and ruler, the stacks the rubric exists for, rate 5/5. **This is why the T15 change must not be described as a strict improvement.** **Not addressed:** the upper-right softness reported alongside the streaking is the capture, not the pipeline — no frame resolves that corner (per-frame detail declines monotonically 0.180 at frame 1 to 0.153 at frame 96, no peak) and frame 1's corner is featureless blur. |
| 2026-08-14 | *see below* | all three | **T14: wavelet-domain stacking (`docs/algorithms.md` §5, Li, Manjunath & Mitra 1995) — built, shipped, and offered in the UI.** CDF 5/3 decimated DWT by lifting, detail coefficients selected by 3x3 area activity on luminance, 3x3 consistency verification, approximation band averaged. Reached by `--method wavelet` and a "Method" combo box. | **SUPERSEDED — do not cite.** Rated ruler 5, synthetic_50 5, blossom 5 at the time; re-rated 4 / 2 / 2 on a verified-clean set on 2026-08-15, see that row. The margin streaking found while reviewing blossom was `warp_frame`'s border clamp, not wavelet's, and is fixed in T15 | **Built as a deliberate test of whether the four-stage architecture generalises. It does not, and that is the finding.** Wavelet needed a fifth trait, `StackFusion`, because focus measurement, weight estimation and fusion collapse into one operation on transform coefficients — there is no per-frame `FocusMap` to hand a `WeightEstimator`. Registration stayed outside and is still shared, so a future registration change remains comparable across both methods. **`Local` is provably untouched:** `output_is_stable` still hashes synthetic_50 to `0x00455c66dd1e4c95`, and `the_app_and_the_cli_agree_byte_for_byte` now covers three configurations (`select`, `blend`, `wavelet`) instead of two. **Outputs to rate:** `target/debug-out/t14/{blossom,ruler}_wavelet.tif`, `t14/synthetic_50_wavelet.tif`, with a matched `synthetic_50_local.tif` beside it; label maps per level under `t14/dbg/` and `t14/synthetic/`. **Measured, matched crops, Local → Wavelet.** *blossom*: background SD 1363.7 → 1475.4 (**+8.2% noisier**), detail Laplacian SD 16764.7 → 18228.4 (**+8.7% sharper**). *ruler*: background SD 1128.0 → 1103.6 (−2.2%), detail 2257.9 → 2383.2 (+5.6%). *synthetic_50 against ground truth*: RMSE 556.5 → 844.1, **52% worse**, on the one stack with an objective reference. **The pattern is coherent rather than mixed:** selecting the highest-activity coefficient sharpens genuine detail and amplifies noise by nearly the same proportion, because in a flat region every frame's activity is noise and the argmax picks the noisiest frame. **The label maps show this directly** and are why they are worth writing: coherent regions over the subject, heavy speckle across smooth bokeh. **Runtime is not the problem** — blossom 244s against Local's ~264s, *faster* despite decomposing every frame twice, because it runs no focus or weights stage and writes no scratch planes. **Memory is the real trade**: peak RSS 6.68 GB (blossom) and 6.50 GB (ruler), against Local's much smaller footprint paid for with ~38 GB of scratch disk. **Two passes are structural, not an oversight:** consistency verification needs the whole label map before filtering it, and the corrected labels then point at coefficients from frames already dropped. **One documented deviation from the paper**, in `defaults::CONSISTENCY_THRESHOLD`: Li et al. fuse two images where a majority of 8 neighbours is always defined; at 100 frames it usually is not, so the filter takes the plurality above a threshold, which reduces to the published majority at two frames — pinned by `two_frames_reduce_to_the_published_majority`. **Checked and *not* a new defect:** the coloured streaking at the left and right frame margins appears in the `Local` output too, so it is pre-existing warp/registration behaviour at frame edges, not something wavelet introduced. **Not verified visually: no app screenshot was taken** — `screencapture` is blocked in this environment. The layout change is covered by `the_panel_swaps_with_the_method`, which asserts presence *and* absence of every control, but that is not the visual check `CLAUDE.md` asks for. |
| 2026-08-10 | *see below* | synthetic_50 | **T13: the fusion rule becomes one type, `core::fusion::FusionKind`, and the app presents it as a combo box. Every rating above still applies — the output is byte-identical.** | n/a — **no quality change, proven twice** | **There were three representations of one choice**: `FusionArg` in the CLI, `FusionRule` in the app, and `Settings.select_fusion: bool`. **They agreed, but by luck of a later edit** — all three read `defaults::SELECT_FUSION`, which only became true at `7df2c2b`; before that they were independent literals with nothing comparing them. That was checked *before* the refactor precisely because unifying types is how a default silently flips, and it is the kind of change that would never show up as a failing test. All three are now gone, replaced by one enum owning the construction and all three user-facing strings. **Proof the output did not move, at both levels.** (1) `tests/output_is_stable.rs` still hashes synthetic_50 to `0x00455c66dd1e4c95`, unchanged. (2) **New gate, `the_app_and_the_cli_agree_byte_for_byte`**: runs the app's own `Run` and then the **CLI binary as a subprocess** on the same stack, for *both* rules, and compares every byte — 6,480,330 bytes identical for `select` and for `blend`. **In-process comparison was rejected deliberately**: it would prove the plumbing agrees with itself, where the thing that has to stay true is that the shipped artifact agrees with the app. Those two paths duplicate scratch handling, frame ordering and parameter routing between `app/run.rs::pipeline` and `cli::run`, and had **never been compared before this**. It passed first time, which is a fact about today, not a guarantee about tomorrow — that is what the test is for. **Three strings, deliberately not collapsed to one:** `token()` (`select`/`blend`) is frozen by this log's own reproducibility, `label()` ("Local"/"Blend") names the mechanism for a photographer, `summary()` states the trade-off. **The enum carries each rule's parameters** — `Select { salience_radius }` — so a third rule adds its knobs to its own variant instead of widening one constructor with arguments the other rules ignore, and `--salience-radius` applied to `blend` is dropped by the type rather than by a constructor politely disregarding it. A test pins them apart, because merging them is the obvious tidy-up that would break either this log or the UI. **UI:** the rule moved from a two-button toggle at the bottom of the panel into a **"Fusion rule"** combo box at the top, with the trade-off shown *on screen* rather than only in a tooltip. **The entry is labelled "Local", not "Selection"** — named for the mechanism (each point decided from its own neighbourhood, against a rule that commits every point to one decision), which avoids promising an outcome and avoids colliding with "select", which in this app already means choosing frames in the filmstrip. **`Blend` is not offered in the app at all.** It measured worse on both real stacks and the log records no case where a photographer should pick it, so it stays CLI-only for reproducing older rows; `UI_FUSION_RULES` is the one-line list that would restore it. Pyramid floor stays visible because **both** rules use it — confirmed from the constructors, not assumed. **The salience radius slider now binds into the `Select` variant**, so a rule without that parameter has no slider to draw rather than one that has to be remembered to hide. |
| 2026-08-10 | `b9691b3` (unmerged) | synthetic_50, blossom | **T12: multi-scale focus measurement (`docs/algorithms.md` §4) — built, measured, and rejected. Not on `main`; the code stays on the unmerged branch `t12-multiscale-focus`.** Windowed Laplacian energy summed across a Gaussian pyramid, `F = F_0 + Σ d^k · expand^k(F_k)`, with CLI flag, UI entry and sweep test. | n/a — **rejected on measurement, no rating taken** | **No rating was taken because there is nothing to look at: blossom differs from the shipped single-scale result by 0.02% RMSE** (17 levels of 65535, per-crop 0.026/0.022/0.019% on florets/rim/bokeh). Asking for a visual score on that would have been asking Niels to adjudicate noise. **Sweeps, same instrument that settled `guide_radius`** (detail = Laplacian energy over the sharpest 2% of ground-truth gradient, bokeh = flattest 50%, identity transforms, all other params shipped defaults): scales **1→5 gives detail 0.330, 0.330, 0.330, 0.330, 0.330**, bokeh 2.393 → 2.383 → 2.383 → 2.384 → 2.385. Scales 1 *is* the shipped metric bit-for-bit, so row 1 is a true control. **Decay swept separately in case 0.5 was suppressing the coarse levels — it was not**: at scales 3, decay 0.25/0.5/1.0/2.0 all give detail 0.330, including decay 2.0 where coarse levels outweigh level 0 eightfold. **A flat sweep usually means a broken experiment, so it was chased rather than reported.** It is not broken, and three measurements say so. (1) *Contribution*: mean focus energy at five scales is **48x** level 0's — the coarse terms dominate the magnitude. (2) *Discrimination*: between-frame contrast, `mean|A−B| / mean((A+B)/2)` for the first and last frame, **rises** with scale count — 0.324, 0.619, 1.190, 1.440, 1.055 — so the coarse levels separate frames *better*, not worse. (3) *Reach*: **96.84% of fused pixels differ** between one scale and five (mean abs 1.4e-3, max 2.3e-2). So the measure changes, the change reaches the output, and the output looks identical anyway. **My first hypothesis was wrong and is recorded as wrong:** I predicted a frame-independent pedestal cancelling in the argmax; measurement (2) refutes it. **The mechanism is not established.** The label field would be the next instrument and was not run. **Suspected cause, with direct evidence but not proof:** levels are summed **unnormalized**, and the 48x figure shows they are not commensurable — the discrete Laplacian's response depends on grid spacing. Scale-space theory normalizes derivatives by scale before combining (Lindeberg, *IJCV* 30(2), 1998). **That is the trigger for any revisit, and it is a different algorithm — a new branch off this dead end, not a resurrection of `t12-multiscale-focus`.** **Cost, for whoever picks it up:** focus stage 16s → 20s on blossom (+25%), ~2% of the run. **Deliberately never built:** max-across-scales. §4 scoped v1 to weighted-sum only rather than leaving an unexposed branch. **What survives on `main`:** §4's write-up of the formulation and these numbers. **What does not:** `MultiScaleLaplacian`, `--focus`, the UI combo entry, and `FOCUS_SCALES`/`FOCUS_DECAY` in `defaults.rs`. **The second-algorithm goal is already met elsewhere** — `select` vs `blend` is a real choice with a measured trade-off (blossom 1→5, synthetic_50 5→4); this was never the thing carrying that. |
| 2026-08-10 | `46b7746` | blossom, synthetic_50 | **Every rating above still applies: the pipeline got 4.6x faster and the output did not move.** LTO, a fix to `reduce`, and parallelism across registration, focus, weights and fusion's inner loops. | n/a — **no quality change, by construction and by check** | **blossom 1222 s → 263 s (20.4 → 4.4 min).** Per stage: register 163 → **18 s** (9.1x), focus 126 → **16 s** (7.9x), weights 316 → **64 s** (4.9x), fuse 617 → **162 s** (3.8x). **The output is the same file.** `cmp` against `t11/blossom_select.tif`, produced before any of this work, reports all 300,676,394 bytes identical, and the reported scale span is unchanged to four decimals (0.9495..1.0569), so registration yields the same transforms rather than similar ones. A byte-identical gate ran after *every* step (`tests/output_is_stable.rs`, synthetic_50, hash `0x00455c66dd1e4c95`): 19.4 → 19.1 → 16.8 → 10.1 → 5.6 → 3.7 s, hash never moving. **This is why the row has no score: a speedup that changes a pixel is an unreviewed algorithm change, and the gate makes that impossible to do by accident.** If it ever fails, the fix is an eval-log row and a rating, not a new constant. **Precondition, measured before any code changed** (`tests/decode_cost.rs`): the claim that decode is CPU-bound was an inference from SSD specs and the whole plan rode on it. On one 301 MB frame — raw read 46 ms cold / 38 ms warm against a 612 ms decode, so **94% of decoding is not I/O**; and 8 frames sequential against 8 *different* frames on 8 threads, disjoint so neither warms the other, **7.6x**. Disk-bound would have shown ~1x. **Two behaviour changes that are not speed.** (1) The four stage traits are now `: Sync`, which `docs/algorithms.md` §15 always assumed and `Image`'s `Mutex` was already built for. (2) **Parallelism weakened what cancellation guarantees in the stages that went wide**: frames already in flight finish, so the guarantee is "no *new* work starts", not "stops at frame N". Fusion is exempt *by design* — its per-frame loop stays sequential because float addition into the accumulator is not associative — which is both why its 6.82 s cancel latency is unchanged and why its tests can still pin an exact frame while the weights test cannot. A cancellation test failed on this and was rewritten to assert the real guarantee rather than the sequential one. **Left on the table:** fusion is now 62% of the run and the least parallel, with `filter::box_sum` and the Laplacian subtract still sequential; registration still decodes every frame twice (198 for 100), which needs pre-decoded grids through `Registration::align` and is now only 7% of the run, so the case has mostly evaporated. |
| 2026-08-10 | `68ca998` | synthetic_50 | **Antenna-softness thread CLOSED — it was the fixture, not the pipeline.** | n/a — closure, and **not** a new measurement | **Closed on Niels's direct read, which is what this log treats as the arbiter.** The residual softness the thread had been chasing is the **staircase aliasing in the ground-truth render** — a property of how the synthetic stack was generated, and *the same artifact already excluded from the score at `2fbf990`*: "not the staircase aliasing in the ground truth itself, which is a rendering artifact and isn't being held against it". It was recorded as excluded from the **rating** and never connected to the **investigation**, so the same artifact went on generating work under a different name for a day. **Do not run the ground-truth-warp-and-remeasure step** named as the next step in the previous status block. It would have measured the fixture more precisely. **What survives, unretracted:** the salience-radius sweep (detail flat at 0.127-0.135 across r0..r3 while bokeh falls 2.406 → 1.384) still refutes the salience window and still justifies the r2 default; the identity-vs-registered gap (0.330 vs 0.134) is still real and still confounded by 1.36 px of mask misalignment. Both remain accurate — they just no longer point at anything worth fixing. `warp_frame`'s bilinear resampling is therefore **unquantified rather than exonerated**: nothing here shows it is costless, only that nothing currently demands the number. **The general lesson, which is the reason this row is long:** a defect that resists explanation is a prompt to re-read the fixture's own known quirks before designing another measurement. `test-data/README.md` documents synthetic_50's compositing artifacts, and the T11 rating had already named this one out loud. Three measurements were designed against something the log had already agreed not to hold against the output. |
| 2026-08-09 | `66fc15b` | ruler | **ruler compared against its Zerene PMax reference** (supplied after the ruler rating, so it fed into nothing above). Also fixes two stale claims in `test-data/README.md`. | n/a — corroboration, already rated 5 | **We are at 95% of PMax's tick contrast, and the black point is corroborated as fixed by an independent implementation.** Tone-independent contrast (p99−p1) over the 600x400 tick region: blend **0.1309**, select **0.3164**, pmax **0.3317**. **Use the range, not the black point, and this is not a nicety:** PMax's whole tonal scale sits lower than ours — p1 0.0857 vs 0.1188, but also p50 0.3819 vs 0.4041 and p99 0.4174 vs 0.4352. Reading p1 alone would credit PMax with 28% deeper blacks when most of that is a global ~0.02 tone offset. Same class of trap as the blossom chroma comparison, where PMax's brighter background inflated an apparent noise gap. **Methodological fix that changed nothing here but would have elsewhere:** PMax sits at **scale 1.06523, dy −12.8** against our output — 82 px away at this crop, 353 px at a corner. The first run compared the same (x,y) in both and so measured *different ticks*; on a repetitive ruler pattern a wrong crop still looks entirely plausible, which is precisely why it needed measuring rather than eyeballing. PMax is now resampled into our frame before anything is cropped or measured (`read_rect_through`), and the numbers moved only in the fourth decimal — the ruler is statistically homogeneous, so the error was invisible in *these* numbers and would not have been in a feature comparison. **Visual, `target/debug-out/t11/crops-ruler/ticks.tif` (`blend | select | pmax`):** blend's ticks are washed-out olive; select's are near-black with ink-edge irregularity; PMax's are comparable to select's, marginally heavier, on a surface showing slightly more grain — consistent with PMax passing through more per-frame noise, as measured on blossom. **Conclusion: the retraction holds.** An independent implementation reaches essentially the same tick contrast, which is what a transfer-function bug on our side could not have produced. |
| 2026-08-09 | `2fbf990` | synthetic_50 | **Open thread: where does the residual antenna softness come from? Two candidates, and the measurement cannot separate them.** Opened rather than closed, per the log's own standing caution. | n/a — investigation, inconclusive by design | **Candidate 1, the salience window — REFUTED.** The window is square and r2 spans 5 px, wider than a 1-3 px antenna, so a neighbouring frame's broader defocused energy could plausibly win the window where the line is sharper at the exact pixel. That predicts detail rising as the radius shrinks. It does not: **r0 0.127, r1 0.133, r2 0.134, r3 0.135** — flat, and if anything *rising* with a wider window. Meanwhile bokeh falls hard with radius (r0 **2.406**, r1 1.634, r2 1.445, r3 1.384). So the window costs no detail and buys substantial noise suppression, which is what §6b claimed and is now measured. **r0 is per-pixel argmax and its bokeh of 2.406 is the concrete price of the rule §6b rejects.** No reason to change the r2 default; r3 is marginally better on both and is worth a look if this is ever revisited. **Candidate 2, bilinear resampling in `warp_frame` — plausible but NOT established.** Every selected coefficient is read out of a bilinearly resampled frame, and bilinear interpolation is a low-pass filter that costs most on exactly 1-3 px structures. Refusing the warp entirely (identity transforms, same rule, same params) gives **detail 0.330 against 0.134 registered** — 93% of the 0.353 oracle versus 38%. **That gap is confounded and must not be quoted as the resampling cost.** The masks come from the unwarped truth while the registered output sits in the anchor frame's coordinates; phase correlation puts them at **scale 1.00150, up to 1.36 px apart at the corners**, and on a 1-3 px antenna a 1 px offset destroys mask overlap with no blurring involved. Both causes are live and this measurement cannot apportion them. **Separating them needs the truth warped into the fused image's coordinates before the masks are cut** — that is the next step if this is picked up, not another energy ratio. |
| 2026-08-09 | `2fbf990` | blossom, ruler, synthetic_50 | **`--fusion` default flipped from `blend` to `select`.** | n/a — ships the rated configuration | Decided on the ratings in the row below: two real-photo stacks gained decisively, one synthetic stress test lost a point. **Reproducing any eval-log row from before this commit now requires an explicit `--fusion blend`** — the flag did not exist before T11 and rows above it were all produced by the blend. Everything else is unchanged: guided radius 4, eps 1e-4, perceptual guide, focus radius 4, pyramid floor 32, salience radius 2. |
| 2026-08-09 | `2fbf990` | blossom, ruler, synthetic_50 | **RATED by Niels at 1:1 — T11 `--fusion select` across all three stacks.** | **blossom: 5 (was 1) · ruler: 5 (was 3) · synthetic_50: 4 (was 5)** | **This is not a clean sweep, and per this log's own rule it is not being averaged away: 1→5 and 3→5 against 5→4.** **blossom 1 → 5**, rated better than the Zerene PMax reference. **ruler 3 → 5**, the lifted black point that held it at 3 resolved — and that defect had been scoped as an unrelated transfer-function thread, which this retracts. **synthetic_50 5 → 4**, one point off for **slightly blurred antennae**. Explicitly *not* counted against it: the staircase aliasing visible in the ground truth itself, which Niels identified as a rendering artifact of the synthetic stack that real photographs will not have — so it is a property of the test fixture, not of the output, and must not be chased. **How to read the trade.** The regression is on the *synthetic* stack and the gains are on both *real photographic* stacks — which is the right way round for a tool whose subject is real macro photography, and is why the default flips despite a stack going down. But the regressing check is thin-structure sharpness, which is the whole point of focus stacking on insects, so this is a genuine cost and not a rounding error. It is logged as an open thread above rather than accepted. **The synthetic stack earned its place here**: it was the only one able to show this at all, because it is the only one with ground truth, and it dissented from two stacks that both rated 5. Keep it. |
| 2026-08-09 | `027c5d2` | ruler | **T11 `--fusion select` on ruler — and it closes the lifted-black-point thread, which turns out not to have been a separate thread at all.** | **needs Niels's rating** — the specific defect that held it at 3 is measurably gone | **The transfer-function hypothesis was wrong, and this row retracts it.** ruler scored 3 held back by low contrast: ticks reading grey with crisp edges. That was filed as a separate thread on the reasoning that crisp edges rule out a sharpness problem and therefore implicate the output transfer function. The reasoning had a hole: **per-level averaging raises a thin dark feature's floor while leaving the finest level — and so the edge — intact**, which predicts "grey but crisp" exactly as well, and blames the fusion rule instead. **Measured, 600x400 tick region at (3000,2900), sRGB-encoded luma percentiles:** p1 (tick ink) **0.2906 → 0.1188**, p99 (yellow body) 0.4214 → 0.4352, so contrast p99−p1 goes **0.1309 → 0.3164, a 2.4x increase**. The floor dropped by 59% while the highlight barely moved — the ticks got darker, the image did not merely get contrastier. p50 is unchanged (0.4000 → 0.4041), so global exposure is undisturbed and checklist item 4 is not at risk. Crop at `target/debug-out/t11/crops-ruler/ticks.tif` (`blend | select`): blend's ticks are washed-out olive, select's are near-black with the ink's edge irregularity and surface texture visible. Also visible at 1/11 scale in `overview_{blend,select}.png` — select's yellow is more saturated and the metal end cap has definition blend loses. **Consequence for the log: `quantize()` in `tiff_io.rs` was never the suspect it was treated as.** It applies `linear_to_srgb` and `round_trips_through_disk` passes, which was already noted as making a plain missing re-encode unlikely; the correct conclusion was that the transfer function was fine and the blend was the cause. One fusion-rule change moved a defect that had been scoped as an unrelated investigation. |
| 2026-08-09 | `027c5d2` | synthetic_50 | **T11 `--fusion select` on the one stack with ground truth** — the stack most likely to disagree, since it scored 5 under the blend. Same params, only the fusion rule changed. | **needs Niels's rating** — my read is better on thin structure, no visible mottling despite the metric | **detail 0.126 → 0.134 (+6%), bokeh 0.730 → 1.445.** **Read the caveat before comparing these to any earlier row: these are NOT on the same scale as the `51d6833` sweep's 0.320.** That sweep ran identity transforms against an unwarped ground truth to isolate the weight stage; these runs use real registration, so the fused image sits in the anchor frame's coordinates while the masks are derived from the unwarped truth. The misalignment depresses `detail` for *both* rules. Only the blend-vs-select comparison **within this row** is valid. **`bokeh` crosses the 1.0 mottling threshold (0.730 → 1.445), and the crops do not show mottling.** Precedent for not treating the number as a verdict: the shipped r4 configuration measured 1.28-1.32 and was rated 5 with no visible mottling, and the sweep row already recorded that whether excess background energy reads as patchy sharpening "is a visual call, not a numeric one". Crops at `target/debug-out/t11/crops-synthetic/` (`blend | select | truth`): *antennae* — select renders the thin black lines **markedly darker and more solid**, close to truth, where blend fades them to grey; this is the same thin-structure win as blossom. *edge* — crisper body/background boundary, no halo, no fringing. *bokeh* (pure corner background, deliberately re-cut after the first attempt clipped the body edge and so measured the edge rather than the background) — **smooth in all three, no patchiness in select**. So the metric records more background energy than the truth has, and the eye does not find it objectionable at 1:1. **Selection wins less here than on blossom, which is expected**: 50 clean frames with mild defocus gave the blend far less to destroy than 100 noisy ISO-1600 frames did, and this stack was already rated 5. The open question is only whether select **regresses** it. |
| 2026-08-09 | `027c5d2` | blossom | **Colour-noise follow-up: is the grain from selection, or from the frames?** Niels reported noticeably more colour noise in `select` than in PMax, which "has almost none" — a chroma complaint the luma-only SD in the row below cannot answer. | n/a — measurement | **Both proposed mechanisms ruled out; the noise is inherited, not manufactured.** (1) *Per-channel selection* — ruled out **by construction**: `select_more_salient` scores salience as `R²+G²+B²` summed and copies the winner's whole RGB triplet, so a pixel can never draw R from one frame and G/B from another. Now enforced by `selection_is_joint_across_channels_never_per_channel`, built so the per-channel winner and the joint winner differ, rather than left true by accident. (2) *Cross-level inconsistency* — the same concern one level up, since each band-pass level selects independently, and this is the mechanism Wang et al. actually warn about. Ruled out by measurement: combining independent noise from different sources would put `select` **~40% above** single-frame level, and it is not. **Chroma SD (BT.601 Cb/Cr, sRGB-encoded, 500x400 background patch), against the source frame at each candidate's own brightness:** blend **−69%** on both, `select` **Cb −1.5% / Cr +7.2%**, pmax **Cb −13.9% / Cr −2.3%**. `select` sits at one frame's chroma noise, which is exactly what selection must do — it takes one frame's coefficients where the blend averaged a hundred and got sqrt(N) suppression. Blend's −69% is that suppression, and it is the same averaging that destroyed the subject; it was never free. **Correction to the framing of the complaint, and it is the interesting part: PMax does not have "almost none".** At matched brightness it is only 2-14% below a single frame. Chroma SD falls **monotonically** with mean Y across these frames (Cb 0.01090 at Y 0.611 down to 0.00895 at Y 0.652), and PMax's patch is 6.4% brighter than ours — so a large part of the visible difference is **tone, not noise**. This is the same trap as measurement error #4 below (noise floor measured at one brightness, applied at another); the raw unmatched numbers overstate the gap by roughly half. **Real remaining gap: ~12 pp on Cb, ~9 pp on Cr.** That is consistent with Zerene applying mild chroma smoothing, and **not** with any difference in fusion architecture — so it is a denoising question, not a reason to revisit the selection rule. Control was 10 frames, not 3: frame-to-frame chroma variation exceeds the margin being judged, and a max over 3 samples would not have been a ceiling. |
| 2026-08-09 | `027c5d2` | blossom | **RATED by Niels: `--fusion select` scores 5/5 — and is judged better than the Zerene PMax reference, not merely comparable.** | **blossom: 5** (was 1) | **The largest single improvement in this log, and the architectural bet paid off.** blossom went **1 → 5** on one change to the fusion rule, with no parameter retuning: guided radius 4, eps 1e-4, focus radius 4, pyramid floor 32 all unchanged from the configuration that scored 1. **This settles the `e2e6b8a` gap that was logged plainly at the time** — that the r4 retune's real, correctly-measured metric gains (signal 0.32 → 0.45, effective frames 25 → 7.6) moved the score from unusable to unusable. They did not fail because the measurements were wrong; they failed because the defect was in the architecture the parameters were tuning, exactly as that row argued. **A per-level selection rule beats a per-pixel weight map propagated to every level, on real 100-frame ISO-1600 macro data, judged against a commercial implementation of the same family.** Beating PMax also retires the worry that PMax was an unreachable ceiling. **Do not read this as licence to skip ratings in favour of metrics** — the reverse: the metrics were right about the parameters and silent about the architecture, and only a human rating distinguished those two cases. |
| 2026-08-09 | `027c5d2` | blossom | **T11: `SelectionFusion`, the per-level windowed-salience rule from `docs/algorithms.md` §6b** (Burt & Kolczynski, *ICCV* 1993). Added alongside `LaplacianPyramidFusion`, selected by `--fusion select`; default stays `blend`. Run: `--fusion select --salience-radius 2`, all other params as the live configuration. | **needs Niels's rating** — my read is a large improvement on items 1 and 3, with noise as a new cost | **The architectural diagnosis is confirmed, and it was visible before any crop was cut.** The `blend` and `pmax` overviews at 1/7 scale (`target/debug-out/t11/crops/overview_*.png`) already separate: blend is globally soft with a dark fringe tracking the whole silhouette, PMax is crisp edge to edge. That is the DMap-shaped weakness §6b predicts, not a tuning miss — no radius/eps value changes what a single weight decision propagates to every level. **Crops** (`target/debug-out/t11/crops/{florets,stalks,rim,bokeh}.tif`, each stitched `blend | select | pmax` at identical coordinates): *florets* — blend is featureless mush; select resolves individual bud hairs and bud shape, structurally comparable to PMax. *rim* — blend's broad bright veil bleeding across the subject edge is **gone** in select, silhouette crisp; this is checklist item 1, the halo, closed on this region. *bokeh* — select is grainier than blend but **uniform, not patchy**; no blotching, so item 3 is not violated by hard selection, which was the main risk flagged when the match/average branch was dropped. **The noise cost, measured rather than eyeballed, because a visual claim in this direction has already been wrong once here:** SD of linear luma over the flat background, blend 1.67% of mean, select 5.22%, **pmax 6.78%**. PMax's background is 15% brighter, and shot noise scales with sqrt(signal), so ~7% of its excess is brightness alone — correcting for it (SD/sqrt(mean)) gives blend 0.0097, select 0.0305, pmax 0.0425. **Select is still meaningfully quieter than the reference Niels rates as good**, so the added grain is within the envelope of an acceptable result rather than a new defect. Blend's smoothness is the same over-averaging that destroys the subject; it is not free. **Coordinate caveat, measured not assumed:** all three are 8664x5784, but phase correlation puts ours against PMax at **scale 0.930, dx -12, dy +10** — Zerene resolved onto a different reference frame. Displacement is ~16 px at the centre and ~380 px at a corner, so `florets` and `rim` (~80-90 px off) are usable for feature comparison and **`stalks` is not** — its panels show different content and that is a registration-anchor difference, not a fusion difference. Do not read `stalks` as evidence either way. **Timing**, 100 frames at 8664x5784: register 163s, focus 126s, weights 316s, fuse 617s, 1232s total — fusion ~8% slower than blend, the salience pass. **Deviations from the paper, both deliberate:** the match/average branch is omitted (it needs every source's coefficients at one level simultaneously — 100 full-res pyramids, unaffordable), and salience is joint across RGB rather than per channel (per-channel selection would fringe the edges the rule exists to fix). The omission is now tracked in `docs/algorithms.md` §6b as a known simplification with a named trigger for revisiting — switching artifacts from near-tied salience, showing as patchy background — in the same form as §10's unmodelled rotation. **Do not implement the match term speculatively.** |
| 2026-08-09 | `e2e6b8a` | synthetic_50 / ruler / blossom | **RATED by Niels at 1:1 — the r4/eps-1e-4 retune.** Supersedes my provisional reads. | **synthetic_50: 5 · ruler: 3 · blossom: 1** | **synthetic_50 = 5**, clean, matches what was already logged. **ruler = 3**, barely acceptable, held back specifically by *low contrast*: ticks read grey rather than black — a lifted black point, not a sharpness problem, since the edges themselves are crisp. Being pursued as a **separate thread** (see the transfer-function row). **blossom = 1**, completely mushy, no usable detail. **Log the gap plainly: the retune's measured gains were real and substantial — signal ratio 0.32 -> 0.45, effective frames 25 -> 7.6 — and the image is still unusable.** A large, correctly-measured improvement moved the score from unusable to unusable. That is the sharpest evidence yet that the metrics track something real but not sufficient, and it argues the remaining problem is architectural rather than a matter of tuning the current architecture further. |
| 2026-08-09 | `c1ee8df` | blossom | **Third-party reference: Zerene Stacker PMax looks good on the same source frames; its DMap-family output looks like ours.** Reported by Niels. | n/a — architectural finding | Zerene's **"Fused" (DMap-family)** output on blossom's actual frames resembles our mushy result; its **"PMax"** output looks very good. These differ *architecturally*, not by tuning. **PMax** builds a resolution pyramid and independently selects the highest-local-contrast source frame **at each pyramid level** — a fresh contrast decision per frequency band. **DMap** makes one depth-map-style frame-selection decision and blends from that single decision; it is documented as weaker on fine detail and prone to exactly this softness, while PMax is documented as strong on "overlapping structures like mats of hair and crisscrossing bristles" — blossom's actual subject. **Stackaroni's T6→T7→T8 matches DMap's shape, not PMax's:** one focus map per frame at a single window scale (T6), smoothed into weight maps (T7), and that same weight map Gaussian-smoothed per level and propagated through the pyramid (T8). One global decision, required to be simultaneously smooth enough to avoid background mottling and sharp enough to keep detail — because it is the only decision there is. PMax has no such tension: each level gets its own independently-scaled decision. **This reframes the radius sweep as tuning a constraint that need not exist**, and demotes it to second priority behind investigating a per-level selection fusion rule. |
| 2026-08-08 | `e30dba2` | ruler | T5: phase-correlation registration (Kuglin & Hines 1975), sub-pixel parabolic peak. | n/a — no fused output yet | **Translation is the wrong model for this data.** Per-region correlation on one adjacent pair: left half +3.32 px, right half −2.98 px; top +0.99, bottom −1.24. At sep=20: +75.5 / −57.8 and +29.6 / −47.7. Opposite-signed and near-symmetric about the centre ⇒ uniform magnification, not displacement. Implied scale change ~0.145%/frame, consistent between sep=1 (6.3 px over 4332 px) and sep=20 (133 px over 4332 px) ⇒ ~14% across 100 frames. Alignment overlay shows red/green doubling growing radially toward the corners. A single `(dx,dy)` averages a field spanning ±3 px, so every adjacent pair keeps ~±3 px residual, ±60 px at the stack ends — ghosting on high-contrast edges (checklist item 2). Predicted by `docs/algorithms.md` §10. Not wired in as the active `Registration` impl. Fix = T5b, log-polar phase correlation (Reddy & Chatterji, *IEEE TIP* 5(8), 1996, 1266–1271) for translation + uniform scale. No rotation or shear in the per-region data, so general affine/ECC is not yet warranted. |
| 2026-08-08 | `4d025b3` | ruler | T5b: similarity registration — log-polar phase correlation for uniform scale (Reddy & Chatterji, *IEEE TIP* 5(8), 1996, 1266–1271). | n/a — no fused output yet | **Fixes the T5 finding.** Per-region spread, full-res px, level 3: sep=1 5.01→1.14, sep=5 27.1→0.96, sep=20 133→35.7. Level 2 sep=1 6.30→0.92. Control (warp one real frame by a *known* similarity, estimate, correct) gives 0.98 px at scale 0.999 and 0.10 px at scale 0.958 — so **~1 px is the estimator's noise floor for small scale changes, not residual misalignment**, and adjacent pairs are now corrected as far as this method can measure. Alignment overlay at sep=20 shows the doubled tick marks collapse to single, radial corner fringing gone. Rotation measured <0.13° on every real pair ⇒ reported via `SimilarityEstimate`, not modelled; ECC affine stays unwarranted. **Two open items for T9 integration:** (1) estimator is ~30× more precise on large scale changes (0.958 → err 3e-5) than small (0.999 → err 2.2e-4), so chaining ~50 small steps out from the anchor may accumulate scale drift — measure direct-to-anchor against chained before wiring in; (2) real pairs at sep=20 keep 36 px spread that the control proves is *not* estimator error, so something beyond a uniform similarity happens over large separations — irrelevant if chaining sep=1 wins, decisive if direct-to-anchor does. |
| 2026-08-09 | `ced3a45` | blossom | **First look at a real stack's argmax label field** (`target/debug-out/t10/debug/blossom/labels_argmax.png`). | n/a — observation | Three things. (1) **The subject's depth structure is coherent** — buds, stems and individual florets resolve as distinct label regions, so the focus measure is producing a sensible depth ordering on real data, not just synthetic. (2) **The entire plain background is salt-and-pepper**, argmax picking essentially at random where there is no detail to measure. Photometrically harmless here because all frames are near-identical in flat background, which is why the fused output shows no mottling despite the label field looking alarming — the same pattern seen on synthetic_50 at `6a1206c`. (3) **New: a bright ring of high-index labels hugs the subject/background boundary.** Likely mechanism is the *focus measure*, not fusion: a radius-4 window centred on background near the subject straddles the subject's edge and inherits its Laplacian energy, so those background pixels get labelled with whichever frame makes that edge sharpest. That is a plausible contributor to the small residual edge overshoot closed out in the row below, and it locates the cause in the focus stage rather than the blend. **Not being chased now** (the overshoot is small and localized, per agreement), but it is the first thing to look at if a rated output ever shows halo at a high-contrast boundary: the fix would be a focus measure that discounts windows straddling a large depth discontinuity, not a change to fusion. |
| 2026-08-09 | `8a3f491` | blossom | **Edge-overshoot question CLOSED: mostly the confound, residual too small to chase.** Warped-source envelope test, `tests/halo_warped_envelope.rs`. | item 1 not violated; question closed | Warping all 100 source frames by their own `Transform` before building the per-position envelope — so fused and sources sit in the same coordinate system — removes the displacement confound the previous test flagged on itself. **Result: above source max at 10/300 positions (was 45/300 unwarped), largest overshoot 10.5% of max at x=4432 (was 23%). Never below source min.** So **78% of the apparent overshoot was inter-frame displacement against a steep edge**, exactly as suspected. The residual 3.3% of positions is clustered at the edge and partly attributable to the ~1 px registration residual (T5b's own measured noise floor) acting on a steep gradient, not necessarily to multi-scale bleed at all. **Stopping here by agreement:** the effect is small, localized, and any real component is well below what the checklist would register; chasing it further is not worth the machine time when the human rating of the actual image is the project's ground truth and has not yet been given. Reopen only if a rated output shows halo at a high-contrast boundary. **Note the read that has NOT been obtained:** Niels has not yet inspected blossom/ruler at 1:1. My own read called item 1 clean, then asserted a halo from one crop, then retracted it — so it carries no evidential weight here and should not be treated as the human rating. |
| 2026-08-09 | `ced3a45` | blossom, ruler, synthetic_50 | **T9 CLI wiring verified byte-for-byte.** Full eval run through the new CLI at r4/eps 1e-4. | n/a — wiring verification | `cmp` reports the CLI outputs **byte-for-byte identical** to the earlier direct-harness run on all three stacks, at matching parameters. That proves the CLI path is equivalent rather than merely producing similar summary statistics. Timings via CLI: blossom 1122s, ruler 1104s, synthetic_50 21s (register/focus/weights/fuse breakdown in `target/debug-out/t10.log`). Per-stage debug output now exists for the real stacks for the first time, at `target/debug-out/t10/debug/<stack>/`: argmax label field, focus and weight planes for first/middle/last frame, alignment overlay, fused preview. |
| 2026-08-09 | `e2e6b8a` | blossom | **Specifics behind the retraction, plus a narrower question it left open.** Amends the retraction below; read both. | unresolved at the edge; background lift refuted | **What was actually measured, since "DOF + downsampling + grain removal" was too vague to audit.** (a) **Mean luminance**, the check that carries the retraction: three patches far from / adjacent to the subject, fused vs sources — fused 1.99% and 1.59% *darker* far out, +0.09% at the boundary; then per-position means across **11 source frames** in patch `160x160+4080+3300`, source range **31.95-40.77**, fused **39.29**, inside the envelope. (b) **Variance**, which explains the visual impression: fused SD ~30% below source (51.6 vs 78.2; 65.0 vs 92.7) — a denoised region reads as milky beside a grainy one without any luminance shift. These are different checks for different defects and both were run; the background-lift claim is refuted by (a), and (b) explains what I mistook for it. **Open question this exposed.** A halo is often a thin rim, which a 160x160 patch mean dilutes. A 300-px luminance profile across the boundary (`300x9+4150+3196`, vertically averaged), against a per-position envelope of **25 source frames**, shows the fused **above the source maximum at 45/300 positions and never below the minimum**, largest overshoot **+19 at x=4447 (fused 103 vs source max 84, 23% above the brightest frame)** — and every overshoot is on the *dark subject side* past the edge at ~4330, none on the bright side. A per-pixel convex combination cannot exceed the source max, so that asymmetry is the signature of multi-scale bleed. **But this test is confounded and does NOT re-establish the halo:** the fused is in anchor coordinates while the sources are unwarped, and with scale spanning 0.9495-1.0569 a point 115 px from centre displaces up to ~7 px between frames — enough, against a steep edge, to put the fused outside a same-x envelope by itself. The patch-mean test in (a) is unaffected because that patch is uniform background well before the edge. **Definitive test, not yet run:** build a per-pixel max over *warped* source frames (apply each frame's `Transform` first, as the pipeline does) and check whether the fused exceeds it near the boundary. That removes the displacement confound entirely. Until then: background lift refuted, edge overshoot unresolved. |
| 2026-08-09 | `e2e6b8a` | blossom | **RETRACTION of the halo finding below. There is no halo.** | item 1 not violated here | Measured immediately after logging it, and it does not hold. **Background level, patch `160x160+4080+3300`:** source frames span **31.95 (A1_00040) to 40.77 (A1_00100)**; fused sits at **39.29 — inside that range**, below the brightest frame. Far from the subject the fused is 1.6-2.0% *darker* than frame 60, and adjacent to the subject it matches within 0.09%. A halo would put the fused above the whole source range; it does not. Background level also trends upward with frame index across the stack (~2.5% spread), consistent with breathing shifting the vignetting, so the fused landing high is just the weighted average favouring the later frames that are sharp there. **What I actually saw:** (a) grain removal — fused SD is ~30% below source (51.6 vs 78.2, 65.0 vs 92.7) — and a denoised smooth region reads as "milky/hazy" beside a grainy one; (b) I compared against a single frame that happens to sit on the dark side of that patch. **So the original question resolves as: blossom's softness is DOF + downsampling + legitimate grain removal, not a pipeline halo.** **Pattern note, and it inverts the previous four:** errors 1-4 were numeric claims corrected by looking at an image. This one is a *visual* claim corrected by measurement — I over-read a side-by-side and asserted a defect that the numbers refute. Neither modality is trustworthy alone; a claim of a *new* defect needs both before it goes in the log. I logged this one too fast, on one crop, having explicitly noted "one crop is not a survey" and then not surveying. |
| 2026-08-09 | `e2e6b8a` | blossom | **Halo at the subject/background boundary — checklist item 1, previously reported as passing.** Found while checking whether blossom's overall softness was haze or shallow DOF. | 1-2 on item 1 for this region — needs Niels's rating | **The haze is not DOF and not a downsampling artifact.** Crop `640x480+4050+3050`, fused vs source frame `A1_00060`, saved at `target/debug-out/blossom_halo_fused_vs_src.png` (plus the two halves as `blossom_halo_fused.png` / `blossom_halo_src_A1_00060.png`). The fused side correctly resolves fine hairs the source lacks — fusion is working — but its background is **noticeably brighter and milkier toward the left**, where the source's background is a cleaner neutral grey. The source has no such lift, so the pipeline introduced it. That is a halo at a high-contrast boundary = **checklist item 1**, which I had previously reported as passing based on the synthetic rim crops. **Likely mechanism:** the classic Laplacian-pyramid blending halo. At coarse pyramid levels the weight map is heavily smoothed (by design — `gaussian_pyramid` of the weights in `fusion.rs`), so across a strong subject/background luminance step, coarse-level energy from the bright background bleeds into the subject's neighbourhood and back. **Why synthetic_50 never exposed it:** its background is mid-green bokeh with no strong luminance step against the subject, whereas blossom is a dark subject against a near-white background — so the whole rim/bokeh/antenna check at `e2e6b8a` was run on data that structurally cannot show this defect. **Two caveats on this finding, stated as given:** (1) a simpler explanation such as an encode/clamp asymmetry in the sRGB round-trip has *not* been ruled out; (2) one crop is not a survey. Both are cheap to check. **Next step:** the T10 run is producing per-stage debug output for blossom for the first time — the weight planes at the subject/background boundary are what would confirm or refute the coarse-level-bleed mechanism. |
| 2026-08-09 | `2f9947f` | blossom | **Amendment: the noise-floor fix itself is approximate.** | n/a — metric critique | The noise floor used for the corrected blossom numbers was measured in a bright flat background crop (`700x600+400+900`) and subtracted from energy measured in a dark subject region (`700x500+4200+3400`). Photon shot noise scales with sqrt(signal), so those two regions do not share a noise level — the floor subtracted is probably the wrong magnitude for the region it was subtracted from, likely over-subtracting. So the "noise-corrected" signal figures are better than the raw ones but are **not** sound; treat them as indicative only. A correct version measures the floor in a flat patch at the *same* brightness as the region under test, or estimates it per-region from a temporally-averaged frame stack. Related to, but distinct from, the third error below: that one is about using raw SD to *rank* frames, this one is about the floor used to *correct* the energy. |
| 2026-08-09 | `2f9947f` | blossom | **Third measurement error: "best source frame" was a defocused frame.** Read before trusting any absolute ratio in the row below. | n/a — metric critique | Ranking source frames by raw Laplacian SD in a region picked `A1_00100` as sharpest at `700x500+4600+3000`. Viewed at 1:1 that frame is almost entirely defocused — blobs and noise — while the fused result resolves individual hairs on the buds, bud shapes and highlights. **Laplacian SD ranks ISO-1600 frames by noise, not focus.** So "fused = 0.45x the best frame" compares against a frame that is not sharp; the absolute ratio is meaningless and the blossom output is visibly *better* than any single frame, as a focus stack should be. The **before/after** in the row below still holds — same flawed denominator on both sides, so 0.32 -> 0.45 reflects a real improvement in the numerator — but the absolute level does not. **Pattern worth naming: three measurement errors in this investigation (missing ceiling control, missing noise floor, noise-dominated frame ranking), all overstating the defect, all caught by looking at the image rather than the number.** A per-frame sharpness ranking that survives noise needs either a noise-matched comparison per region or ranking by the focus map the pipeline already computes, not by raw Laplacian SD. Until then, rank frames visually. |
| 2026-08-09 | `e2e6b8a` | blossom, ruler, synthetic_50 | **Retune: guided filter radius 8 -> 4, eps 1e-2 -> 1e-4.** Re-ran all three stacks. | improved, pending Niels's rating | **After, against the `51d6833` before.** blossom, noise-floor-subtracted signal (Laplacian SD with the flat-region noise floor removed in quadrature): best source frame 10596, fused **3398 -> 4753**, ratio **0.32 -> 0.45** (+40%). **Effective frames averaged per pixel: ~25 -> 7.6**, from the noise drop and noise falling as sqrt(N) — this is the clearest single readout of how selective the weights are, and worth keeping as a standing measurement. Ideal is 1-3, so headroom remains. synthetic_50 detail 0.195 -> 0.320 against a 0.353 per-pixel oracle, i.e. **55% -> 91% of the physical maximum**. Visual checks at r4 on synthetic_50 crops (`target/debug-out/candidates/`): bokeh shows no mottling (checklist 3 pass), rim shows no halo and no ghosting (1, 2 pass), antennae clearly darker and better resolved than r8. Ruler at 1:1 on the tick marks is markedly sharper than a defocused source frame with clean edges and no doubling. **Chosen r4 over r8/1e-4** (which scored detail 0.256, bokeh 1.07) because neither showed mottling visually, so the detail gain was free. **Open:** 7.6 effective frames on blossom vs ~1-3 ideal suggests the optimal radius is data-dependent — blossom's noisier label field spreads weight further than synthetic's at the same radius — which argues for exposing radius/eps as CLI params (already planned for T9) rather than hardcoding one value. No ruler before/after: no baseline was taken, by agreement. |
| 2026-08-09 | `51d6833` | synthetic_50 | **Correction to the row below, plus guide-radius sweep with a proper ceiling control.** | n/a — measurement | **The `51d6833` blossom row overstates the defect; read this first.** That row compared fused Laplacian SD against *source frames* and called 0.25x "detail destruction". The comparison is confounded: ISO-1600 frames carry heavy noise which inflates their Laplacian SD, and averaging 100 frames legitimately removes it. How much of the 0.25x was lost detail versus removed noise is **not yet known** — it needs a noise-robust measure on blossom. **Sweep on synthetic_50** with two purpose-built metrics (detail = Laplacian energy over the sharpest 2% of ground-truth gradient; bokeh = same over the flattest 50%; 1.00 = matches truth): radius 1 → detail 0.329 / bokeh 1.65; r2 → 0.329 / 1.50; r4 → 0.305-0.320 / 1.28-1.32; r8 → 0.195-0.256 / 0.86-1.07; r16 → 0.092-0.152 / 0.46-0.71. **Ceiling control, which makes the sweep interpretable:** best single source frame = 0.286, per-pixel oracle (sharpest frame at every pixel, unachievable) = **0.353**. The ground truth is a perfect all-in-focus render, sharper than any real frame, so detail can never reach 1.00. **So radius 1-2 achieves 0.329 = 93% of the physical maximum — the pipeline is sound.** The shipped radius 8 / eps 1e-2 gives 0.195 = **55% of the oracle**, discarding ~45% of achievable detail. That part of the diagnosis stands. **The two failure modes do trade off**, as expected: bokeh crosses 1.00 (mottling threshold) between r8 and r4, exactly where detail starts recovering. Best compromise in this sweep is **r8/eps 1e-4 (detail 0.256 = 73% of oracle, bokeh 1.07)** or **r4/eps 1e-4 (0.320 = 91%, bokeh 1.32)**; which is preferable depends on how visible 30% excess bokeh energy is, which is a visual call, not a numeric one. Note r16 and r8/1e-2 give bokeh *below* 1.0 — smoother than the truth, i.e. over-smoothing the background too. **Not yet grounds for graph-cut**: a usable middle appears to exist, contrary to the possibility that none would. |
| 2026-08-09 | `51d6833` | blossom, ruler | **First full-pipeline run on the real 50 MP stacks.** registration -> focus -> weights -> fusion, params: reg level 3, focus radius 4, guide radius 8, eps 1e-2, pyramid floor 32. Outputs at `test-data/<stack>/stackaroni_fused.tif`. | 1-2 — **bad, gross defect** | **Baseline before the sharpness retune; keep these numbers to compare against.** Sharpness as Laplacian SD, fused vs the sharpest source frame in the same region (blossom): centre 3061 vs 12480 = **0.25x**; corner 2550 vs 12504 = **0.20x**. Output is 4-5x *less* detailed than its own inputs. Some loss is legitimate (averaging 100 ISO-1600 frames genuinely denoises) but not this much. Visually: globally soft, hazy, with a bright halo around the subject against the white background. Not formally scoring ruler separately — same root cause plausibly explains both, re-score both together after the fix. **Cause, partly separated:** centre-vs-corner splits the two candidates. If chained registration drift were dominant, the centre (accumulated transform ~identity) should be near source sharpness — it is not (0.25x), so drift is **not** primary. Corner being worse than centre (0.20 vs 0.25) still implicates it as a real secondary contributor, confirming T5b open item #1 (chained scale drift) as output-visible. **Primary suspect: over-smoothed weights.** Guide radius 8 at eps 1e-2 spreads each frame's one-hot map over a neighbourhood; after normalization every pixel draws on every frame that won anywhere nearby, and with noisy labels that is many frames. That is averaging, which is what the numbers show. Same softening seen on synthetic_50 at `6e638d7`, far worse here (100 noisy frames vs 50 clean). **Registration itself is sound** and this is worth having confirmed rather than assumed: scale span 0.9495..1.0569 (blossom) and 0.9535..1.0542 (ruler), ~11% magnification consistently across two independently shot stacks — exactly the breathing T5b was built for. Timings, 100 frames at 8664x5784: register 163s, focus 127s, weights 281s, fuse 572s, 1144s total, 38 GB scratch. |
| 2026-08-09 | `6e638d7` | synthetic_50 | T8: Laplacian-pyramid fusion (Burt & Adelson 1983; Burt & Kolczynski 1993). **First fused output.** | 3 (provisional) | 50 frames in 12.6 s, mean abs error 0.00397 vs ground truth. Worst rows 264-271, **contiguous**, 2.48x median — feature-localized, not the periodic isolated rows a band seam would give (there are no bands: fusion accumulates frame-by-frame, 2.47 GB peak vs 16 GB banded). Checklist from 1:1 crops against ground truth: **(1) partial** — no seams, no halos, no rim fringing, but thin dark structures are *attenuated*, antennae render grey and soft where truth has them thin and black; **(2) pass** — no ghosting, weak test at identity transforms; **(3) pass cleanly** — bokeh smooth, no mottling, despite the argmax label field being pure salt-and-pepper there, so guided filter + multi-scale blending absorbed it entirely; **(4) pass** — no colour/exposure shift. Dominant defect is global softening, worst on thin structures — the same defect that `51d6833` shows far more severely on the real stacks. |
| 2026-08-09 | `6e638d7` | synthetic_50 | Second failure of ground-truth RMSE as an arbiter. | n/a — metric critique | T7 concluded edge RMSE improves monotonically with *more* guided-filter smoothing, and eps=1e-2 was chosen as a middle on that basis. That setting is now the prime suspect for the softening on both synthetic_50 and the real stacks. The RMSE was almost certainly rewarding noise reduction, not sharpness — averaging more frames lowers error on a noisy render while destroying line contrast. Combined with the T7 finding that RMSE under-weights background coherence, ground-truth RMSE has now pointed the wrong way twice, **in opposite directions**. Use it for gross regressions only; thin-structure contrast and background coherence each need purpose-built measures or visual rating. |
| 2026-08-09 | `6a1206c` | synthetic_50 | T7: guided-filter weight refinement (He, Sun & Tang, *ECCV* 2010; Li, Kang & Hu, *IEEE TIP* 22(7), 2013). | n/a — no fused output yet | Scored against `ground_truth_all_in_focus.tiff` by RMSE, split into overall and the top-2% gradient pixels (the thin antenna/leg structures). No filter: 0.00993 / 0.05178. Guided best: 0.00930 (ε=1e-4) / 0.04952 (ε=1e-1). **The stage earns its place** — beats raw argmax on both. Mild trade: edge error falls monotonically with more smoothing while overall error bottoms at ε=1e-4, so ~1e-2 is a reasonable middle until fusion exists to re-check. Guide radius 8, focus radius 4. |
| 2026-08-09 | `6a1206c` | synthetic_50 | Guide space for the guided filter: linear light vs sRGB-encoded. | n/a — no difference found | **Prior not supported here, but the test may lack the condition.** Expectation was that gamma-encoded luma would win, because linear radiance spans a wider numeric range so a dark antenna against mid-tone can have smaller linear edge magnitude than a minor step inside a highlight — biasing the filter toward protecting highlights over thin dark structures. Measured: the two track within 0.04% at *every* ε, on both metrics. Caveat: a synthetic render may simply not have the tonal spread that makes the argument bite; `blossom` (real ISO-1600 bokeh highlights) is where it would show. Kept `GuideSpace` enum, defaulted to `Perceptual` on theory. **Re-test on blossom once T8 fusion exists.** *Methodology note:* the first comparison used a single ε and was invalid — ε is not scale-invariant, sRGB values being numerically larger means the same ε smooths less, so it varied guide space and smoothing strength together. Sweeping ε is what separates them. |
| 2026-08-09 | `6a1206c` | synthetic_50 | Limitation of ground-truth RMSE as the arbiter. | n/a — metric critique | **Don't trust this metric for background coherence.** The argmax label field (`labels_argmax.png`) shows violent salt-and-pepper across the whole bokeh region — neighbouring pixels selecting unrelated frames, exactly §7's failure and checklist item 3. Yet cleaning it up moves RMSE by ~6%. Reason: in smooth bokeh every frame is photometrically near-identical, so a wrong label costs almost nothing in RMSE. The metric is a good instrument for edge fidelity and a poor one for background coherence — visual rating stays the arbiter there. Body's vertical depth-band seams also visible in the label field, matching the documented synthetic-stack compositing artifact. |
| 2026-08-08 | `e30dba2` | ruler | Benchmark design: antisymmetry as a registration-accuracy measure. | n/a — measure discarded | **Didn't work, don't retry.** Scored `align(a,b)` against `−align(b,a)`, expecting it to expose defocus-driven correlation error. Measured ~1e-4 px at every level and separation — useless. Reason: phase correlation is antisymmetric *by construction*; swapping arguments conjugates the cross-power spectrum, so the peak negates exactly regardless of how wrong it is. It measures the algebra, not the data. Chain-vs-direct (3.5–5.4 px disagreement over 8 frames) and per-region spread did the real work. |
| 2026-08-08 | `e30dba2` | ruler | Registration accuracy vs pyramid level, injected known shift. | n/a — estimator check | Mean error in full-res px: level 1 → 0.38, level 2 → 0.92, level 3 → 2.03, level 4 → 4.42. On *shared* content finer is strictly better, so the hypothesis that downsampling improves accuracy by suppressing defocus-mismatched high frequencies is **not** supported for level choice on its own — the dominant error source turned out to be the wrong motion model, not frequency mismatch. Revisit level choice after T5b. Timing per pair: level 1 ≈ 7.1 s, level 3 ≈ 1.6 s, level 4 ≈ 1.3 s (mostly frame decode, which is level-independent). |

<!--
Tip: if a change scores worse on one stack but better on another, don't
average it away — note the tradeoff explicitly so it doesn't get relitigated
later without remembering why.
-->

